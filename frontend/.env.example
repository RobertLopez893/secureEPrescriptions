# URL base del backend FastAPI.
# En desarrollo local con docker-compose el backend escucha en el puerto 8000.
# En otros entornos apunta al dominio público (https://api.midominio.com).
PUBLIC_API_URL=http://localhost:8000
